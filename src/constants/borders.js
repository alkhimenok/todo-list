import { BASE_SIZE } from '@constants/units'

export const THIN = `${parseInt(BASE_SIZE) * 0.2}rem solid`
export const THICK = `${parseInt(BASE_SIZE) * 0.6}rem solid`
export const RADIUS_CIRCLE = '50%'
export const RADIUS_OVAL = `${parseInt(BASE_SIZE) * 5}rem`
export const RADIUS_SUPERELLIPSE = `${parseInt(BASE_SIZE) * 1}rem`
